"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { ArrowLeft, ArrowRight, Sparkles } from "lucide-react";
import { createClient } from "@/lib/supabase/client";

export default function SignupPage() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setMessage(""); const client = createClient(); if (!client) { setMessage("Add your Supabase environment variables to enable account creation."); setLoading(false); return; } const { error } = await client.auth.signUp({ email, password, options: { data: { full_name: name } } }); setMessage(error ? error.message : "Check your email to confirm your account."); setLoading(false); } return <main className="flex min-h-screen items-center justify-center bg-[#f7f8f3] px-6 py-12"><div className="w-full max-w-md"><Link href="/" className="mb-12 flex items-center gap-2 text-sm text-[#63706b]"><ArrowLeft size={15} /> Back to OnePost</Link><div className="mb-8 flex items-center gap-2 font-semibold"><span className="flex h-8 w-8 items-center justify-center rounded-[10px] bg-[#14211f] text-[#d7f36a]"><Sparkles size={16} /></span>OnePost</div><h1 className="text-4xl font-semibold tracking-[-.04em]">Make room for your ideas.</h1><p className="mt-3 text-sm text-[#63706b]">Your publishing workspace starts here.</p><form onSubmit={submit} className="mt-9 space-y-5"><label className="block text-sm font-medium">Your name<input required value={name} onChange={e => setName(e.target.value)} className="mt-2 w-full rounded-xl border border-[#d5ded3] bg-white px-4 py-3 outline-none focus:border-[#8aa431]" /></label><label className="block text-sm font-medium">Email<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-[#d5ded3] bg-white px-4 py-3 outline-none focus:border-[#8aa431]" /></label><label className="block text-sm font-medium">Password<input required minLength={8} type="password" value={password} onChange={e => setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-[#d5ded3] bg-white px-4 py-3 outline-none focus:border-[#8aa431]" /></label>{message && <p role="status" className="rounded-xl bg-[#eef5d9] px-4 py-3 text-sm text-[#526a20]">{message}</p>}<button disabled={loading} className="w-full rounded-full bg-[#14211f] py-3.5 text-sm font-medium text-white disabled:opacity-50">{loading ? "Creating..." : <>Create account <ArrowRight className="ml-1 inline" size={15} /></>}</button></form><p className="mt-8 text-center text-sm text-[#63706b]">Already have an account? <Link href="/auth/login" className="font-semibold text-[#14211f]">Sign in</Link></p></div></main>; }