"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { ArrowLeft, Sparkles } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
export default function ForgotPasswordPage() { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const client = createClient(); if (!client) { setMessage("Add your Supabase environment variables to enable password recovery."); return; } const { error } = await client.auth.resetPasswordForEmail(email, { redirectTo: `${window.location.origin}/auth/reset-password` }); setMessage(error?.message ?? "Check your email for a secure reset link."); } return <main className="flex min-h-screen items-center justify-center bg-[#f7f8f3] px-6"><div className="w-full max-w-md"><Link href="/auth/login" className="mb-12 flex items-center gap-2 text-sm text-[#63706b]"><ArrowLeft size={15} /> Back to sign in</Link><div className="mb-8 flex items-center gap-2 font-semibold"><span className="flex h-8 w-8 items-center justify-center rounded-[10px] bg-[#14211f] text-[#d7f36a]"><Sparkles size={16} /></span>OnePost</div><h1 className="text-4xl font-semibold tracking-[-.04em]">Reset your password.</h1><p className="mt-3 text-sm text-[#63706b]">We will send a secure link to your inbox.</p><form onSubmit={submit} className="mt-9 space-y-5"><label className="block text-sm font-medium">Email<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-[#d5ded3] bg-white px-4 py-3 outline-none focus:border-[#8aa431]" /></label>{message && <p role="status" className="rounded-xl bg-[#eef5d9] px-4 py-3 text-sm text-[#526a20]">{message}</p>}<button className="w-full rounded-full bg-[#14211f] py-3.5 text-sm font-medium text-white">Send reset link</button></form></div></main>; }